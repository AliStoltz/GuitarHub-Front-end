import React from 'react';

import './Landing.css';

const Landing = (props) => {
  return(
    <div className="landing-container">
      <h1>Julian sux!</h1>
    </div>
  )
};

export default Landing;